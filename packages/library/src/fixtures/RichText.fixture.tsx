import React from 'react';

import { FixtureBox, RichTextEditor } from '../components';

const RichTextFixture = () => {
    return (
        <FixtureBox hasPadding>
            <RichTextEditor />
        </FixtureBox>
    );
};

export default RichTextFixture;
